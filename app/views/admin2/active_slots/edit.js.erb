var popup = $('#slotAddModal');
if (popup.length) {
    popup.remove();
}

$('body').append('<%= j render 'admin2/active_slots/add_popup', category: @category, shapes: @shapes, selected_shape_ids: @selected_shape_ids %>');
popup.modal('show');
validateCategory();
disableSelectAll();